;; e2wm

;; (add-to-list 'load-path "~/.emacs.d/site-lisp/emacs-window-layout")
;; (add-to-list 'load-path "~/.emacs.d/site-lisp/emacs-window-manager")
;; (require 'e2wm)

;; (e2wm:start-management)
;; (setq e2wm:c-code-recipe
;;   '(| (:left-max-size 25)
;;       (- (:upper-size-ratio 0.7)
;;          files history)
;;       (- (:upper-size-ratio 0.7)
;;          (| (:right-max-size 30)
;;             main imenu)
;;          sub)))
